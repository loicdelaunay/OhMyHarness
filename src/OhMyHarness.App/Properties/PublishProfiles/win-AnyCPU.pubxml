<Project>
  <PropertyGroup>
    <!-- Default Uno Windows profile; the publish script selects the target architecture. -->
    <WindowsPackageType>None</WindowsPackageType>
    <PublishTrimmed>false</PublishTrimmed>
  </PropertyGroup>
</Project>
